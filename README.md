# Display a nested tree of data

State management was conducted by using react hooks, basic unit testing was conducted.

To run, first download node_modules:

`yarn`

To test:

`yarn test`

To start:

`yarn start`
